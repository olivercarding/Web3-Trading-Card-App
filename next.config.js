/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: "",
  images: {
    domains: ['cryptokaiju.io'], // Add any other domains as needed
  },
};

module.exports = nextConfig;
